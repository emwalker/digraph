import { ReactNode } from 'react';
declare type Props = {
    children: ReactNode;
    placeholder: string;
    hasItems: boolean;
};
declare const _default: ({ children, hasItems, placeholder }: Props) => JSX.Element;
export default _default;
