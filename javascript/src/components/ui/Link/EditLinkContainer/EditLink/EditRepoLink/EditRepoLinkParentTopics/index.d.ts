declare type Props = {
    linkId: string;
    viewerId: string;
    selectedRepoId: string;
};
export default function ParentTopics({ selectedRepoId, viewerId, ...rest }: Props): JSX.Element;
export {};
