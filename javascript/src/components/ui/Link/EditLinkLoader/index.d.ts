declare type Props = {
    linkId: string;
    viewerId: string;
    toggleForm: () => void;
};
export default function EditLinkLoader({ linkId, toggleForm, viewerId }: Props): JSX.Element;
export {};
