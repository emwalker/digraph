declare type Props = {
    toggleForm: () => void;
    topicId: string;
    viewerId: string;
};
export default function EditTopicLoader({ toggleForm, topicId, viewerId }: Props): JSX.Element;
export {};
