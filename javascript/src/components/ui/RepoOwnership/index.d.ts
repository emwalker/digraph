import { Color } from 'components/types';
declare type Props = {
    showRepoOwnership: boolean;
    repoColors: Color[];
};
export default function RepoOwnership(props: Props): JSX.Element | null;
export {};
