type PageInfo {
  hasNextPage: Boolean!
  hasPreviousPage: Boolean!
  startCursor: String
  endCursor: String
}

type User {
  id: ID
  resourceIdentifier: String!
  name: String!
  email: String!
}

type Topic {
  id: ID
  resourceIdentifier: String!
  organization: Organization!
  name: String!
  description: String
}

type TopicEdge {
  cursor: String!
  node: Topic
}

type TopicConnection {
  edges: [TopicEdge]
  pageInfo: PageInfo!
}

type Organization {
  id: ID
  resourceIdentifier: String!
  name: String!
  topics(
    first: Int,
    after: String,
    last: Int,
    before: String
  ): TopicConnection
}

type Query {
  viewer: User
  organization(resourceIdentifier: String!): Organization
}

type Mutation {
  createUser(email: String!): User
  removeUser(id: ID!): Boolean
}
