import React from 'react';
import { Match } from 'found';
import { Account_view$key } from '__generated__/Account_view.graphql';
type Props = {
    match: Match;
    view: Account_view$key;
};
export default function Account(props: Props): React.JSX.Element | null;
export {};
