import React from 'react';
import { Color } from 'components/types';
type Props = {
    showRepoOwnership: boolean;
    repoColors: Color[];
};
export default function RepoOwnership(props: Props): React.JSX.Element | null;
export {};
