import React, { ReactNode } from 'react';
type Props = {
    children: ReactNode;
    placeholder: string;
    hasItems: boolean;
};
declare const _default: ({ children, hasItems, placeholder }: Props) => React.JSX.Element;
export default _default;
